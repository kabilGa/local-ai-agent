# Gateway

The API Gateway - the single entry point for the system. Receives all client requests, authenticates them, validates input, routes to the right backend service (router, rag, sandbox, security), and returns responses. Owner: Adam.
