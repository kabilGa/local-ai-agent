[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: mark test as requiring live infrastructure (Qdrant, Redis)
    unit: pure unit test, no external dependencies
addopts =
    -v
    --tb=short
    -m "not integration"
